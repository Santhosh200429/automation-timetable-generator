services:
  - type: web
    name: php-app
    env: docker
    plan: free
    region: oregon
    buildCommand: ""
    startCommand: "php -S 0.0.0.0:10000 -t ."
    envVars:
      - key: PORT
        value: "10000"
