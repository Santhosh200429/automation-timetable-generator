services:
  - type: web
    name: automation-timetable
    env: docker
    repo: https://github.com/Santhosh200429/automation-timetable-generator
    plan: free
    branch: main
    rootDir: .
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 10000
